{
  "name": "dev-web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix Back-entreprise & npm run dev --prefix Front-entreprise",
    "dev:back": "npm run dev --prefix Back-entreprise",
    "dev:front": "npm run dev --prefix Front-entreprise",
    "install:all": "npm install --prefix Back-entreprise && npm install --prefix Front-entreprise"
  }
}
